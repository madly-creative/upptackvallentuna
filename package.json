{
  "name": "upptack-vallentuna",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "prebuild": "node scripts/generate-seo.mjs && node scripts/check-producer-pages.mjs public/verksamhet",
    "predev": "node scripts/generate-seo.mjs",
    "build": "vite build && node scripts/check-producer-pages.mjs dist/verksamhet",
    "generate:seo": "node scripts/generate-seo.mjs",
    "check:producers": "node scripts/check-producer-pages.mjs",
    "report:unmatched": "node scripts/report-unmatched-links.mjs",
    "generate:icons": "node scripts/generate-pwa-icons.mjs",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test",
    "typecheck": "node -e \"console.log('no TS yet')\""
  },
  "devDependencies": {
    "@playwright/test": "^1.54.2",
    "jsdom": "^26.1.0",
    "vite": "^7.0.6",
    "vitest": "^3.2.4"
  },
  "dependencies": {
    "nodemailer": "^6.10.1"
  }
}
